<UserControl x:Class="clamshield_antivirus.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:clamshield_antivirus.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Settings" Style="{StaticResource HeadingText}"/>
            <TextBlock Text="Configure ClamUI behavior and integrations" Style="{StaticResource SubheadingText}" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- Explorer Integration -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Explorer Integration" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                        <TextBlock Text="Add or remove the 'Scan with ClamUI' option from the Windows Explorer right-click context menu for files, folders, and drives." 
                                   TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                        <Border Padding="16" CornerRadius="8" Background="{StaticResource SidebarBgBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Context Menu" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                                    <TextBlock Text="Right-click files/folders and scan instantly" FontSize="12" Foreground="{StaticResource DimTextBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" IsChecked="{Binding ContextMenuEnabled}" 
                                              Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Auto-Start with Windows -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Startup" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                        <TextBlock Text="Automatically launch ClamUI when you sign in to Windows." 
                                   TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                        <Border Padding="16" CornerRadius="8" Background="{StaticResource SidebarBgBrush}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Launch at Startup" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                                    <TextBlock Text="Start ClamUI automatically when you log in" FontSize="12" Foreground="{StaticResource DimTextBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <ToggleButton Grid.Column="1" IsChecked="{Binding StartupEnabled}" 
                                              Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Scheduled Scanning -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Scheduled Scanning" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                        <TextBlock Text="Schedule automatic scans via Windows Task Scheduler. The app will scan all drives at the configured time." 
                                   TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                        <Border Padding="16" CornerRadius="8" Background="{StaticResource SidebarBgBrush}">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Enable Scheduled Scan" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                                        <TextBlock Text="Run scans automatically on a schedule" FontSize="12" Foreground="{StaticResource DimTextBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <ToggleButton Grid.Column="1" IsChecked="{Binding ScheduleEnabled}" 
                                                  Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                                </Grid>

                                <Grid Margin="0,0,0,8" IsEnabled="{Binding ScheduleEnabled}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Time:" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" 
                                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <TextBox Grid.Column="1" Text="{Binding ScheduleTime}" Width="100" HorizontalAlignment="Left" FontSize="13"/>
                                </Grid>

                                <Grid Margin="0,0,0,8" IsEnabled="{Binding ScheduleEnabled}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding ScheduleWeekly}" 
                                              Content="Weekly" Foreground="{StaticResource SecondaryTextBrush}"
                                              VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    <ComboBox Grid.Column="1" SelectedIndex="{Binding ScheduleDay}" Width="120" 
                                              Visibility="{Binding ScheduleWeekly, Converter={StaticResource BoolToVis}}">
                                        <ComboBoxItem Content="Sunday"/>
                                        <ComboBoxItem Content="Monday"/>
                                        <ComboBoxItem Content="Tuesday"/>
                                        <ComboBoxItem Content="Wednesday"/>
                                        <ComboBoxItem Content="Thursday"/>
                                        <ComboBoxItem Content="Friday"/>
                                        <ComboBoxItem Content="Saturday"/>
                                    </ComboBox>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Scan Options -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Scan Options" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                        <TextBlock Text="Configure limits and file types for scanning." 
                                   TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                        <Border Padding="16" CornerRadius="8" Background="{StaticResource SidebarBgBrush}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Maximum File Size Limit" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                                        <TextBlock Text="Skip scanning files exceeding this size limit" FontSize="12" Foreground="{StaticResource DimTextBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBox Text="{Binding MaxFileSizeMB, UpdateSourceTrigger=PropertyChanged}" Width="80" TextAlignment="Right" FontSize="13"/>
                                        <TextBlock Text=" MB" Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Real-time Monitoring -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Real-time Protection" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                        <TextBlock Text="Monitor folders for new or modified files and scan them automatically for threats." 
                                   TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                        <Border Padding="16" CornerRadius="8" Background="{StaticResource SidebarBgBrush}">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Enable Real-time Monitoring" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                                        <TextBlock Text="Automatically scan new and modified files" FontSize="12" Foreground="{StaticResource DimTextBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <ToggleButton Grid.Column="1" IsChecked="{Binding RealtimeEnabled}" 
                                                  Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                                </Grid>

                                <TextBlock Text="Files scanned: {Binding RealtimeScanned}" FontSize="12" Foreground="{StaticResource DimTextBrush}" 
                                           Visibility="{Binding RealtimeEnabled, Converter={StaticResource BoolToVis}}"/>

                                <Separator Background="{StaticResource SeparatorBrush}" Margin="0,12"/>

                                <TextBlock Text="Watched Folders" FontWeight="SemiBold" FontSize="13" Foreground="White" Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding WatchFolders}" Margin="0,0,0,8">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" 
                                                           VerticalAlignment="Center"/>
                                                <Button Grid.Column="1" Content="✕" FontSize="10" Width="20" Height="20"
                                                        Background="Transparent" BorderThickness="0"
                                                        Foreground="{StaticResource DimTextBrush}"
                                                        Command="{Binding DataContext.RemoveWatchFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding NewWatchPath, UpdateSourceTrigger=PropertyChanged}" 
                                             FontSize="12" Margin="0,0,8,0"/>
                                    <Button Grid.Column="1" Content="Browse" 
                                            Command="{Binding BrowseWatchFolderCommand}" Margin="0,0,8,0"
                                            Style="{StaticResource SecondaryButton}" Padding="12,4"/>
                                    <Button Grid.Column="2" Content="Add" 
                                            Command="{Binding AddWatchFolderCommand}"
                                            Style="{StaticResource SecondaryButton}" Padding="12,4"/>
                                </Grid>

                                <Separator Background="{StaticResource SeparatorBrush}" Margin="0,12"/>

                                <TextBlock Text="Exclusion Patterns" FontWeight="SemiBold" FontSize="13" Foreground="White" Margin="0,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding ExclusionPatterns}" Margin="0,0,0,8">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" 
                                                           VerticalAlignment="Center"/>
                                                <Button Grid.Column="1" Content="✕" FontSize="10" Width="20" Height="20"
                                                        Background="Transparent" BorderThickness="0"
                                                        Foreground="{StaticResource DimTextBrush}"
                                                        Command="{Binding DataContext.RemoveExclusionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding NewExclusion, UpdateSourceTrigger=PropertyChanged}" 
                                             FontSize="12" Margin="0,0,8,0"/>
                                    <Button Grid.Column="1" Content="Add" 
                                            Command="{Binding AddExclusionCommand}"
                                            Style="{StaticResource SecondaryButton}" Padding="12,4"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
