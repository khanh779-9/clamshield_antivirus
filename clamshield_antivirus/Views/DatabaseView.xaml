<UserControl x:Class="clamshield_antivirus.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:clamshield_antivirus.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=vm:DatabaseViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Database Update" Style="{StaticResource HeadingText}"/>
                <TextBlock Text="Update ClamAV virus definitions to detect the latest threats" Style="{StaticResource SubheadingText}" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Status Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,6,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📦" Style="{StaticResource CardIcon}"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Updater Status" Style="{StaticResource CardSubtitle}"/>
                        <TextBlock Text="{Binding StatusText}" Style="{StaticResource CardTitle}" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                        <TextBlock Text="C# Update Engine Active" FontSize="11" Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="6,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="⏰" Style="{StaticResource CardIcon}"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Last Database Update" Style="{StaticResource CardSubtitle}"/>
                        <TextBlock Text="{Binding LastUpdateTime}" Style="{StaticResource CardTitle}" Margin="0,4,0,0"/>
                        <TextBlock Text="Definitions are active" FontSize="11" Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Console Log Output" Style="{StaticResource SectionTitle}"/>
            <TextBox Grid.Row="1" 
                     Text="{Binding ProgressLog, Mode=OneWay}" 
                     FontFamily="{StaticResource MonoFont}"
                     FontSize="12"
                     Background="{StaticResource SidebarBgBrush}"
                     Foreground="{StaticResource PrimaryTextBrush}"
                     BorderBrush="{StaticResource BorderBrush}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextChanged="TextBox_TextChanged"/>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center"
                        Visibility="{Binding IsUpdating, Converter={StaticResource BoolToVis}}">
                <Grid Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Grid.RenderTransform>
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                     From="0" To="360" Duration="0:0:1.2" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>
                    <Ellipse Stroke="{StaticResource BorderBrush}" StrokeThickness="2" Opacity="0.5"/>
                    <Ellipse Stroke="{StaticResource AccentBrush}" StrokeThickness="2" StrokeDashArray="3 1.5"/>
                </Grid>
                <TextBlock Text="Downloading &amp; parsing database definitions. Please wait..." Style="{StaticResource CardSubtitle}" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel Update" Style="{StaticResource DangerButton}" Command="{Binding CancelUpdateCommand}" Margin="0,0,8,0"
                        Visibility="{Binding IsUpdating, Converter={StaticResource BoolToVis}}"/>
                <Button Content="Update Definitions" Style="{StaticResource AccentButton}" Command="{Binding UpdateCommand}" 
                        IsEnabled="{Binding CanUpdate}"
                        Visibility="{Binding IsUpdating, Converter={StaticResource InvBoolToVis}}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
