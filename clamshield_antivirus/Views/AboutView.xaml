<UserControl x:Class="clamshield_antivirus.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:clamshield_antivirus.ViewModels"
             xmlns:helpers="clr-namespace:clamshield_antivirus.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[Settings.AboutTitle]}" Style="{StaticResource HeadingText}"/>
            <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppDesc]}" Style="{StaticResource SubheadingText}" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource SectionCard}" Margin="0,0,0,0">
                    <StackPanel>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" 
                                    Background="{StaticResource SurfaceBrush}" 
                                    BorderBrush="{StaticResource AccentBrush}" 
                                    BorderThickness="2" 
                                    CornerRadius="40" 
                                    Width="64" 
                                    Height="64" 
                                    VerticalAlignment="Top" 
                                    Margin="0,0,20,0">
                                <TextBlock Text="🛡️" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppTitle]}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
                                <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppDesc]}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[Settings.AboutVersionLabel]}" FontSize="11" Foreground="{StaticResource DimTextBrush}"/>
                                <TextBlock Text="{Binding AppVersion}" FontSize="11" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,10"/>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppVersionLabel]}" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding AppVersion}" FontWeight="SemiBold" Foreground="White" FontSize="12"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.CoreEngineLabel]}" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.EngineVersionVal]}" Foreground="White" FontSize="12"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.DbSourceLabel]}" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.DbSourceVal]}" Foreground="White" FontSize="12"/>
                        </Grid>

                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,8"/>

                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LegalTitle]}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LegalDesc]}" FontSize="11" TextWrapping="Wrap" Style="{StaticResource CardSubtitle}" Margin="0,0,0,6"/>

                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LeadDevLabel]}" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding Author}" FontWeight="SemiBold" Foreground="White" FontSize="12"/>
                        </Grid>

                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="GitHub:" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="{Binding GithubUrl}" Foreground="#4A90E2" FontSize="12"/>
                        </Grid>

                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.Copyright]}" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
