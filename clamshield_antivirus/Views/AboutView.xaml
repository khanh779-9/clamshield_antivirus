<UserControl x:Class="clamshield_antivirus.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:clamshield_antivirus.ViewModels"
             xmlns:helpers="clr-namespace:clamshield_antivirus.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.Title]}" Style="{StaticResource HeadingText}"/>
            <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.Subtitle]}" Style="{StaticResource SubheadingText}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Main Info Container -->
        <Grid Grid.Row="1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Brand Icon (Shield) -->
            <Border Grid.Column="0" 
                    Background="{StaticResource SurfaceBrush}" 
                    BorderBrush="{StaticResource AccentBrush}" 
                    BorderThickness="2" 
                    CornerRadius="60" 
                    Width="120" 
                    Height="120" 
                    VerticalAlignment="Top" 
                    Margin="0,0,32,0">
                <Grid>
                    <TextBlock Text="🛡️" FontSize="56" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <!-- Small virus icon overlay -->
                    <Border Background="#F38BA8" CornerRadius="12" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="12,12">
                        <TextBlock Text="🦠" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppTitle]}" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppDesc]}" FontSize="13" Foreground="{StaticResource SecondaryTextBrush}" Margin="0,0,0,16"/>

                <Border Style="{StaticResource SectionCard}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.SystemSpecs]}" Style="{StaticResource SectionTitle}"/>

                        <!-- Detail Rows -->
                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.AppVersionLabel]}" Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding AppVersion}" FontWeight="SemiBold" Foreground="White"/>
                        </Grid>
                        
                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,6"/>

                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.CoreEngineLabel]}" Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding EngineVersion}" Foreground="White"/>
                        </Grid>

                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,6"/>

                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.DbSourceLabel]}" Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding DatabaseInfo}" Foreground="White"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LegalTitle]}" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LegalDesc]}" 
                                   TextWrapping="Wrap" Style="{StaticResource CardSubtitle}" Margin="0,0,0,12"/>
                        
                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,6"/>

                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Source={x:Static helpers:LocalizationService.Instance}, Path=[About.LeadDevLabel]}" Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Author}" FontWeight="SemiBold" Foreground="White"/>
                        </Grid>
                        
                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,6"/>

                        <Grid Margin="0,4,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="GitHub:" Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Grid.Column="1" Text="{Binding GithubUrl}" Foreground="#4A90E2"/>
                        </Grid>

                        <Separator Background="{StaticResource SeparatorBrush}" Margin="0,6"/>
                        
                        <TextBlock Text="{Binding CopyrightText}" Foreground="{StaticResource DimTextBrush}" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
