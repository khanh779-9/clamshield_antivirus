<UserControl x:Class="clamshield_antivirus.Views.ProtectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:clamshield_antivirus.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=vm:ProtectorViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Real-Time Protector" Style="{StaticResource HeadingText}"/>
                <TextBlock Text="Continuous system-wide filesystem protection running with Administrator privileges" Style="{StaticResource SubheadingText}" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0"
                    Background="{Binding ProtectionStatus, Converter={StaticResource StatusToColor}, ConverterParameter='card_bg'}"
                    BorderBrush="{Binding ProtectionStatus, Converter={StaticResource StatusToColor}, ConverterParameter='card_border'}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="🛡️" Style="{StaticResource CardIcon}" FontSize="36"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Real-Time Shield Status" Style="{StaticResource CardSubtitle}"/>
                        <TextBlock Text="{Binding CurrentStatusText}" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"
                                   Foreground="{Binding ProtectionStatus, Converter={StaticResource StatusToColor}}"
                                   Style="{StaticResource CardTitle}"/>
                        <TextBlock Text="Active System Shield (Admin)" Style="{StaticResource CardSubtitle}" Margin="0,2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <ToggleButton Style="{StaticResource ToggleSwitchStyle}" IsChecked="{Binding IsRunning, Mode=TwoWay}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Files Checked" Style="{StaticResource CardSubtitle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding TotalScanned}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource InfoBrush}" VerticalAlignment="Center"/>
                    </Grid>

                    <Separator Grid.Row="1" Background="{StaticResource SeparatorBrush}" Margin="0,4"/>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Threats Blocked" Style="{StaticResource CardSubtitle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding ThreatsBlocked}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Monitored Areas" Style="{StaticResource SectionTitle}"/>
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="12">
                    <Grid>
                        <ListBox ItemsSource="{Binding MonitoredPaths}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="💾" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="System Drive (Full Access)" FontSize="10" Style="{StaticResource CardSubtitle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="16"
                                    Visibility="{Binding IsRunning, Converter={StaticResource InvBoolToVis}}">
                            <TextBlock Text="⚠️" FontSize="32" HorizontalAlignment="Center"/>
                            <TextBlock Text="Shield Offline" Style="{StaticResource CardTitle}" HorizontalAlignment="Center" Margin="0,8,0,0" FontSize="15"/>
                            <TextBlock Text="Enable Real-Time Protection to begin monitoring system drives." 
                                       Style="{StaticResource CardSubtitle}" TextAlignment="Center" MaxWidth="200" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Column="1" Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Recent Activity Log" Style="{StaticResource SectionTitle}"/>
                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding ActivityLogs}"
                         Background="{StaticResource SidebarBgBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         BorderThickness="1"
                         FontFamily="{StaticResource MonoFont}"
                         FontSize="11"
                         Padding="6"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Padding" Value="2"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Converter={StaticResource StringContainsConverter}, ConverterParameter='[WARNING]'}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
